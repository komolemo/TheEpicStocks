version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3001:3000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=file:/app/dev.db

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
